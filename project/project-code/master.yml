swagger: "2.0"
info: 
  version: "0.0.1"
  title: "spamInfo"
  description: "An intelligent system to determine whether a given email is spam or not"
  termsOfService: "http://swagger.io/terms/"
  contact: 
    name: "Spamalot"
  license: 
    name: "Apache"
host: "localhost:8080"
basePath: "/"
schemes: 
  - "http"
consumes: 
  - "application/json"
produces: 
  - "text/html"
paths: 
  /upload:
    post:
      operationId: py_scripts.upload.upload
      description: "Uploads a file"
      responses:
        "201":
          description: "Upload"  
  #/model:
  #  get:
  #    operationId: py_scripts.model.showModel
  #    description: "Shows the model"
  #    responses:
  #      "200":
  #        description: "Model"
